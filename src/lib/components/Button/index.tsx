import style from "./style.module.scss";

const Button = () => {
  return (
    <>
      <button type="button" class={style.button}>
        Button
      </button>
    </>
  );
};

export default Button;
